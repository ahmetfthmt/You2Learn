import { Button } from "@/components/ui/button";
import { Progress } from "@/components/ui/progress";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import type { LearningPayload } from "@shared/learning";
import { CheckCircle2, ChevronLeft, ChevronRight, CircleHelp, Clock3, Lightbulb, ListChecks, Target } from "lucide-react";
import { useMemo, useState } from "react";

export function LearningView({ payload }: { payload: LearningPayload }) {
  const [revealedCheckpoints, setRevealedCheckpoints] = useState<number[]>([]);
  const [examIndex, setExamIndex] = useState(0);
  const [answers, setAnswers] = useState<Record<number, number>>({});
  const question = payload.exam.questions[examIndex];
  const isAnswered = answers[question.id] !== undefined;
  const score = useMemo(() => Object.entries(answers).reduce((sum, [id, answer]) => sum + (payload.exam.questions.find((item) => item.id === Number(id))?.correctIndex === answer ? 1 : 0), 0), [answers, payload.exam.questions]);

  return <section className="min-w-0 rounded-3xl border border-slate-200 bg-white shadow-[0_20px_60px_-30px_rgba(15,23,42,0.28)]"><div className="border-b border-slate-100 px-5 py-6 sm:px-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-indigo-600">Kaynak sadakat özeti</p><h1 className="mt-2 text-2xl font-semibold tracking-tight text-slate-950 sm:text-3xl">{payload.lesson.title}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">{payload.sourceSummary}</p></div><div className="flex shrink-0 items-center gap-2 rounded-xl bg-slate-950 px-3 py-2 text-sm font-medium text-white"><Clock3 className="size-4" /> {payload.estimatedMinutes} dk</div></div><div className="mt-5 rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm leading-6 text-amber-950"><strong>Kaynak sınırı:</strong> {payload.sourceBoundaries}</div></div><Tabs defaultValue="ders" className="p-5 sm:p-8"><TabsList className="grid h-11 w-full grid-cols-2 rounded-xl bg-slate-100 p-1"><TabsTrigger value="ders" className="gap-2 rounded-lg"><ListChecks className="size-4" />Ders Akışı</TabsTrigger><TabsTrigger value="sinav" className="gap-2 rounded-lg"><Target className="size-4" />Ustalık Sınavı</TabsTrigger></TabsList><TabsContent value="ders" className="mt-6"><div className="mb-7 rounded-2xl bg-indigo-50 p-5"><div className="flex items-center gap-2 font-semibold text-indigo-950"><Target className="size-5" />Öğrenme hedefleri</div><ul className="mt-3 grid gap-2 text-sm leading-6 text-indigo-900">{payload.lesson.learningObjectives.map((objective, index) => <li key={`${objective}-${index}`} className="flex gap-2"><CheckCircle2 className="mt-1 size-4 shrink-0 text-indigo-600" />{objective}</li>)}</ul></div><div className="space-y-5">{payload.lesson.modules.map((module, index) => { const revealed = revealedCheckpoints.includes(index); return <article key={`${module.title}-${index}`} className="rounded-2xl border border-slate-200 p-5 sm:p-6"><div className="flex items-start gap-4"><span className="grid size-8 shrink-0 place-items-center rounded-full bg-slate-950 font-mono text-sm font-bold text-white">{index + 1}</span><div className="min-w-0"><h2 className="text-lg font-semibold text-slate-950">{module.title}</h2><p className="mt-1 text-sm font-medium text-indigo-700">{module.objective}</p></div></div><p className="mt-5 text-sm leading-7 text-slate-700">{module.explanation}</p><div className="mt-5 grid gap-2 border-l-2 border-indigo-200 pl-4">{module.keyPoints.map((point, pointIndex) => <div key={`${point}-${pointIndex}`} className="text-sm leading-6 text-slate-700">{point}</div>)}</div><div className="mt-6 rounded-xl bg-slate-50 p-4"><div className="flex gap-2 text-sm font-semibold text-slate-900"><CircleHelp className="size-4 text-indigo-600" />Kendini kontrol et</div><p className="mt-2 text-sm leading-6 text-slate-700">{module.checkpoint.question}</p>{revealed ? <div className="mt-3 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-sm leading-6 text-emerald-950"><strong>Yanıt:</strong> {module.checkpoint.answer}<br /><span className="text-emerald-900">{module.checkpoint.explanation}</span></div> : <Button variant="outline" onClick={() => setRevealedCheckpoints((items) => [...items, index])} className="mt-3 rounded-lg border-slate-300 bg-white">Yanıtı göster</Button>}</div></article>; })}</div></TabsContent><TabsContent value="sinav" className="mt-6"><div className="mb-6 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-semibold text-slate-950">Ustalık Sınavı (Mastery Exam)</h2><p className="mt-1 text-sm text-slate-600">{payload.exam.introduction}</p></div><span className="rounded-full bg-slate-100 px-3 py-1.5 font-mono text-sm text-slate-700">{Object.keys(answers).length}/{payload.exam.questions.length} yanıt · {score} doğru</span></div><Progress value={(Object.keys(answers).length / payload.exam.questions.length) * 100} className="h-2" /><article className="mt-6 rounded-2xl border border-slate-200 p-5 sm:p-6"><div className="flex items-center justify-between gap-3"><span className="font-mono text-xs font-semibold uppercase tracking-[0.15em] text-indigo-600">Soru {examIndex + 1} / {payload.exam.questions.length}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-600">{question.competency}</span></div><h3 className="mt-4 text-lg font-semibold leading-7 text-slate-950">{question.question}</h3><div className="mt-5 grid gap-3">{question.options.map((option, optionIndex) => { const selected = answers[question.id] === optionIndex; const correct = question.correctIndex === optionIndex; const state = isAnswered ? (correct ? "border-emerald-500 bg-emerald-50 text-emerald-950" : selected ? "border-rose-400 bg-rose-50 text-rose-950" : "border-slate-200 bg-white text-slate-500") : "border-slate-200 bg-white text-slate-800 hover:border-indigo-400 hover:bg-indigo-50/30"; return <button key={`${option}-${optionIndex}`} type="button" disabled={isAnswered} onClick={() => setAnswers((current) => ({ ...current, [question.id]: optionIndex }))} className={`flex w-full items-start gap-3 rounded-xl border p-4 text-left text-sm leading-6 transition ${state}`}><span className="grid size-6 shrink-0 place-items-center rounded-full border border-current font-mono text-xs">{String.fromCharCode(65 + optionIndex)}</span><span>{option}</span></button>; })}</div>{isAnswered && <div className={`mt-5 rounded-xl p-4 text-sm leading-6 ${answers[question.id] === question.correctIndex ? "bg-emerald-50 text-emerald-950" : "bg-rose-50 text-rose-950"}`}><strong>{answers[question.id] === question.correctIndex ? "Doğru." : "Bu seçenek doğru değil."}</strong><br />{question.explanation}</div>}<div className="mt-6 flex justify-between gap-3"><Button variant="outline" disabled={examIndex === 0} onClick={() => setExamIndex((index) => index - 1)} className="gap-1 rounded-xl"><ChevronLeft className="size-4" /> Önceki</Button><Button disabled={examIndex === payload.exam.questions.length - 1} onClick={() => setExamIndex((index) => index + 1)} className="gap-1 rounded-xl bg-slate-950 hover:bg-slate-800">Sonraki <ChevronRight className="size-4" /></Button></div></article>{Object.keys(answers).length === payload.exam.questions.length && <div className="mt-6 flex items-start gap-3 rounded-2xl border border-indigo-200 bg-indigo-50 p-5 text-indigo-950"><Lightbulb className="mt-0.5 size-5 shrink-0 text-indigo-600" /><p className="text-sm leading-6"><strong>Sınav tamamlandı:</strong> {payload.exam.questions.length} soruda {score} doğru yanıt verdiniz. Yanıtlarınızı gözden geçirmek için sorular arasında gezinebilirsiniz.</p></div>}</TabsContent></Tabs></section>;
}
